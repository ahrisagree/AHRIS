import React from 'react';
import {
  Paper,
  Container,
} from '@material-ui/core';

const Home = props => {

  return (
    <Container component={Paper}>
      <h1>WELCOME</h1>
    </Container>
  )
};

export default Home;  
